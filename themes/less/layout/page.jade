extends base-left
block title
  title= page.title + ' | ' + config.title
block content
  .post
    h1.post-title= page.title
    .post-content
      != page.content
      te
  if page.comments
    include _partial/comments
